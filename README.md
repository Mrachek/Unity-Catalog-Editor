# Unity-Catalog-Editor
Simple Databricks Unity Catalog Editor that uses a service principle to view and update catalogs, schemas, and related objects.

Connection details are saved locally after a successful login and can be reselected from the saved connections list on startup.
